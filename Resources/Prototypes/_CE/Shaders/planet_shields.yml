# CrystallEdge: procedural hex dome drawn over a planet disc while its shield is
# up (CEPlanetOverlay). Fully textureless — the whole pattern is shader math.
- type: shader
  id: CEPlanetShieldSkin
  kind: source
  path: "/Textures/_CE/Shaders/shield_skin.swsl"
